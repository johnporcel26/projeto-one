import { useEffect, useRef } from "react"; import Phaser from "phaser"; import { WorldScene } from "./WorldScene"; import { BuffaloPreviewScene } from "./BuffaloPreviewScene";
export function GameCanvas(): JSX.Element { const host = useRef<HTMLDivElement>(null); useEffect(() => { if (!host.current) return; const preview = new URLSearchParams(window.location.search).has("buffalo-preview"); const game = new Phaser.Game({ type: Phaser.AUTO, parent: host.current, width: 960, height: 640, backgroundColor: "#152820", scene: preview ? [BuffaloPreviewScene] : [WorldScene], scale: { mode: Phaser.Scale.RESIZE, autoCenter: Phaser.Scale.CENTER_BOTH } }); return () => game.destroy(true); }, []); return <div className="game-canvas" ref={host} />; }
