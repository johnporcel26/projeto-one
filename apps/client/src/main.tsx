import { useEffect, useMemo } from "react"; import { fruitDefinitions, itemDefinitions } from "@onepiece/shared"; import { createRoot } from "react-dom/client"; import { GameCanvas } from "./game/GameCanvas"; import { bridge } from "./game/GameBridge"; import { GameSocket } from "./network/GameSocket"; import { Hud } from "./components/hud/Hud"; import { GameUiProvider, useGameUi } from "./ui/GameUiStore"; import "./theme/pirate-theme.css"; import "./styles.css"; import "./theme/pirate-hud.css";
function GameApp(): JSX.Element { const socket = useMemo(() => new GameSocket(), []); const { setSnapshot, addLog } = useGameUi(); useEffect(() => { socket.onSnapshot = (snapshot) => { for (const definition of snapshot.contentCatalog.items) itemDefinitions[definition.id] = definition; for (const definition of snapshot.contentCatalog.fruits) fruitDefinitions[definition.id] = definition; bridge.snapshot = snapshot; bridge.onSnapshot?.(snapshot); setSnapshot(snapshot); }; socket.onLog = addLog; socket.connect(); const listener = (event: Event) => socket.intent((event as CustomEvent).detail); window.addEventListener("game-intent", listener); return () => window.removeEventListener("game-intent", listener); }, [socket, setSnapshot, addLog]); return <main className="game-app"><GameCanvas /><Hud onIntent={(intent) => socket.intent(intent as never)} /></main>; }
createRoot(document.getElementById("root")!).render(<GameUiProvider><GameApp /></GameUiProvider>);
